# 04 Agosto 2025

`Nuove funzionalità`&#x20;

### API v. 2, secondo rilascio

Vengono aggiunti 29 nuovi endpoint di lettura e scrittura su vari domini, e integrati i parametri di query di 3 endpoint già rilasciati in precedenza. Di seguito il dettaglio degli endpoint. Per la documentazione completa, si guardi il [documento di interfaccia aggiornato](https://developer.pagopa.it/pdnd-interoperabilita/api/pdnd-core-v2#/).

#### E-service (e-service)

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>POST /reversePurposeeservicePurposes</td><td>Carica una nuova analisi del rischio per un tuo e-service di erogazione inversa in stato di bozza</td></tr></tbody></table>

#### Richieste di fruizione (agreement)

<table><thead><tr><th width="270.49609375">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /agreements/{agreementId}/purposes</td><td>Restituisce le finalità associate ad una richiesta di fruizione</td></tr><tr><td>GET /agreements/{agreementId}/contract</td><td>Restituisce il documento PDF che attesta l'attivazione della richiesta di fruizione</td></tr><tr><td>GET /agreements/{agreementId}/consumerDocuments</td><td>Restituisce la documentazione che un fruitore ha associato ad una richiesta di fruizione. La documentazione caricata viene utilizzata dall'erogatore per conferire al fruitore uno o più attributi verificati</td></tr><tr><td>GET /agreements/{agreementId}/consumerDocuments/{documentId}</td><td>Restituisce uno specifico documento caricato per una richiesta di fruizione</td></tr><tr><td>POST /agreements/{agreementId}/clone</td><td>Clona una richiesta di fruizione</td></tr><tr><td>POST /agreements/{agreementId}/consumerDocuments</td><td>Carica uno o più documenti per una richiesta di fruizione. La documentazione caricata viene utilizzata dall'erogatore per conferire al fruitore uno o più attributi verificati</td></tr><tr><td>DELETE /agreements/{agreementId}</td><td>Elimina una propria richiesta di fruizione in bozza</td></tr><tr><td>DELETE /agreements/{agreementId}/consumerDocuments/{documentId}</td><td>Elimina uno specifico documento caricato per una richiesta di fruizione in stato di bozza</td></tr></tbody></table>

#### Finalità (purpose)

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /purposes/{purposeId}/agreement</td><td>Restituisce la richiesta di fruizione associata ad una finalità</td></tr><tr><td>GET /purposes/{purposeId}/versions/{versionId}/riskAnalysisDocument</td><td>Restituisce il contenuto dell'analisi del rischio associata ad una versione di finalità. Viene creata una nuova versione di finalità ogni volta che si altera la stima di carico</td></tr><tr><td>DELETE /purposes/{purposeId}</td><td>Elimina una propria finalità in stato di bozza</td></tr><tr><td>DELETE /purposes/{purposeId}/versions/{versionId}</td><td>Elimina una propria versione di finalità in stato di bozza</td></tr></tbody></table>

#### Attributi (attribute)

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /certifiedAttributes</td><td>Restituisce la lista degli attributi certificati</td></tr><tr><td>GET /declaredAttributes/{attributeId}</td><td>Restituisce uno specifico attributo dichiarato</td></tr><tr><td>GET /verifiedAttributes/{attributeId}</td><td>Restituisce uno specifico attributo verificato</td></tr><tr><td>POST /declaredAttributes</td><td>Crea un nuovo attributo dichiarato</td></tr><tr><td>POST /verifiedAttributes</td><td>Crea un nuovo attributo verificato</td></tr></tbody></table>

#### Deleghe (delegation)

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /producerDelegations</td><td>Restituisce le deleghe in erogazione conferite dal proprio ente</td></tr><tr><td>POST /producerDelegations</td><td>Crea una nuova richiesta di delega per l'erogazione verso un altro ente (potenziale delegato)</td></tr><tr><td>POST /producerDelegations/{delegationId}/accept</td><td>Accetta una delega in erogazione inoltrata da un altro ente</td></tr><tr><td>POST /producerDelegations/{delegationId}/reject</td><td>Rifiuta una delega in erogazione inoltrata da un altro ente</td></tr></tbody></table>

#### Client (client)

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /clients</td><td>Restituisce la lista dei client del proprio ente</td></tr><tr><td>GET /clients/{clientId}</td><td>Restituisce il dettaglio di un client del proprio ente</td></tr><tr><td>GET /clients/{clientId}/purposes</td><td>Restituisce la lista di finalità alle quali un client è associato</td></tr><tr><td>GET /clients/{clientId}/keys</td><td>Restituisce la lista delle chiavi di un client</td></tr><tr><td>DELETE /clients/{clientId}/purposes/{purposeId}</td><td>Disassocia un client da una finalità</td></tr></tbody></table>

#### Ente (tenant)

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /tenants/{tenantId}/declaredAttributes</td><td>Restituisce la lista degli attributi dichiarati di un ente</td></tr><tr><td>GET /tenants/{tenantId}/verifiedAttributes</td><td>Restituisce la lista degli attributi verificati di un ente</td></tr></tbody></table>

#### Endpoint integrati

Per 3 endpoint di GET già pubblicati nel primo rilascio si inseriscono gli ultimi parametri di query utili a richiedere risultati filtrati. I 3 endpoint sono:

<table><thead><tr><th width="270.70703125">Endpoint</th><th>Descrizione</th></tr></thead><tbody><tr><td>GET /eservices</td><td>Restituisce una lista di e-service</td></tr><tr><td>GET /agreements</td><td>Restituisce una lista di richieste di fruizione</td></tr><tr><td>GET /purposes</td><td>Restituisce una lista di finalità</td></tr></tbody></table>
